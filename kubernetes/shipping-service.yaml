apiVersion: v1
kind: Service
metadata:
  name: shipping
  labels:
    app: shipping
spec:
  type: NodePort
  ports:
  - name: server
    port: 8086
    protocol: TCP
    targetPort: 8086
  selector:
    app: shipping